export const selectDrinks = (state) => state.drinks.drinks;
export const selectTotalCount = (state) => state.drinks.totalCount;
export const selectDrinksIsLoading = (state) => state.drinks.isLoading;
