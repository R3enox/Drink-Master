import styled from 'styled-components';

export const Header = styled.div`
  justify-content: space-between;
  position: sticky;
  
  border-style: outset;
  top: 0;
 
  display: flex;
  z-index: 99;
`;
